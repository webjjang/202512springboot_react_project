<<외부 아이피로 접근 설정>>

<react ip로 접근>
npm run dev -- --host 실행
또는 vite.config.js 파일 수정 -> npm run dev 로 실행
export default defineConfig({
  server: {
    host: "0.0.0.0",
    port: 5173,
  },
})

<react에서 Spring Boot로 접근 설정>
- spring boot URL을 등록해 놓고 사용한다.
\webjjang_front\.env 파일 생성 작성 --> 잘 적용이 안됨.
VITE_API_URL=http://172.30.1.52
component에서 아래와 같이 사용
import.meta.env.VITE_API_URL

api의 baseURL: "http://172.30.1.52" 로 설정해서 사용 강추

<Spring Boot 접근 설정>
- application.properties 설정 추가
 server.address=0.0.0.0
- SecurityConfigurations
        configuration.setAllowedOrigins(
                List.of(
                        "http://localhost:5173",
                        "http://127.0.0.1:5173",
                        "http://172.30.1.52:5173"
                )
        );

